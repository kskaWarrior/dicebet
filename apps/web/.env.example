# Copie para apps/web/.env para `npm run dev:web`. O GoTrue é o do repo `rgs`
# (rollout RGS, porta 57331, compartilhado com os jogos irmãos) — não mais um próprio.
NUXT_PUBLIC_API_BASE=http://localhost:8080
NUXT_PUBLIC_AUTH_URL=http://127.0.0.1:57331
